# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

app-example

# cache y binarios de Gradle/C++ que se recrean en cada build
android/.gradle/
android/**/build/
android/.cxx

# Cache de Gradle en la raiz cuando ejecutas tareas fuera de /android.
.gradle/

# Ajustes de IDE (Android Studio/VS Code) personales.
.idea/
.vscode/
*.iml

# Informes de test y logs de ejecucion.
coverage/
*.log

# Guarda capturas de iconos/splash generadas automáticamente por Expo y el estado de configuración del simulador; se regenera al vuelo y no es código fuente.
.expo-shared/

# Carpeta local que crea la CLI cuando usas emuladores; contiene bases de datos temporales y configuraciones específicas de tu máquina.
.firebase/

# Logs de la CLI (firebase deploy, emulador, etc.). Útiles para depurar, pero cambian en cada ejecución y no son parte del proyecto.
firebase-debug.log*

# Fichero generado por Gradle con la ruta local de tu SDK de Android; es distinto en cada ordenador y no debe compartirse.
android/local.properties

# Dependencias compiladas de tus Cloud Functions. Se reinstalan con npm install dentro de functions/; subirlas hincha el repo sin aportar código.
functions/node_modules/

# Salida JavaScript transpilada desde TypeScript (tsc). Se vuelve a generar en cada despliegue (firebase deploy) y no debería versionarse.
functions/lib/